services:
  portfolio:
    build: .
    container_name: portfolio-tracker
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Kolkata
