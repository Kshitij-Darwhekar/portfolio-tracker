# Shell scripts must keep LF line endings so they run on Linux (the Pi),
# even when committed from Windows. CRLF would break the shebang line.
*.sh text eol=lf
# .env.example is copied to .env and sourced by bash — CRLF would corrupt values.
.env.example text eol=lf
